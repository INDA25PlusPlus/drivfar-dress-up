# Drivfar Dress-up

## Building
Dependencies: You must have CSFML and `clang-format` installed.

Please run the following to make sure that formatting is automatically checked before commiting:
```shell
git config core.hooksPath .githooks
```
